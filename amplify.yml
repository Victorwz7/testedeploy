version: 1
applications:
  - appRoot: src
    frontend:
      framework: nextjs
      artifacts:
        baseDirectory: .next  # Certifique-se de que a pasta .next seja usada
        files:
          - '**/*'
